{
  "name": "OOC Java Labs",
  "image": "mcr.microsoft.com/devcontainers/java:1-21-bookworm",
  "customizations": {
    "codespaces": {
      "openFiles": ["labs/README.md"]
    },
    "vscode": {
      "extensions": [
        "vscjava.vscode-java-pack"
      ],
      "settings": {
        "java.project.sourcePaths": ["labs/src"]
      }
    }
  },
  "postCreateCommand": "java -version",
  // Each time the workspace opens: catch up with your repo on GitHub (what the
  // nightly update committed) and, while the two agree, bring in any newer
  // lecture or lab-instruction fix and save it straight back. Content only --
  // it never touches your own code -- and it is quiet and never fails, so it
  // can't stop the Codespace starting if you are offline. In the module's own
  // repo it does nothing.
  "postAttachCommand": "bash scripts/update-course-content.sh --attach || true"
}
